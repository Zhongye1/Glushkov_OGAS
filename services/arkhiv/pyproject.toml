[project]
name = "ogas-arkhiv"
version = "0.0.0"
description = "OGAS 知识面：基于 EagleRAG fork 的 MCP Server（Milvus + workspace 权限封装）"
requires-python = ">=3.11"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/arkhiv"]

[tool.ruff]
line-length = 100
