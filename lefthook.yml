pre-commit:
  parallel: true
  commands:
    oxlint:
      glob: "*.{js,jsx,ts,tsx}"
      run: pnpm exec oxlint --fix {staged_files}
    oxfmt:
      glob: "*.{js,jsx,ts,tsx,mjs,cjs}"
      run: pnpm exec oxfmt {staged_files}
